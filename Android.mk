# Intentionally empty.
#
# This repository contains kernel/vendor build makefiles that are consumed by
# Kleaf rather than the Android platform build. Keeping an Android.mk at the
# repository root prevents the platform findleaves scan from recursively
# including those kernel-specific makefiles.
